import { createSlice } from "@reduxjs/toolkit";

const PageDescSlice = createSlice({
  name: "PageDesc",
  initialState: false,
  reducers: {},
});

export const {} = PageDescSlice.actions;
export default PageDescSlice.reducer;
